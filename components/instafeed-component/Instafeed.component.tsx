import { InstaItem } from "@/sections/footer-section/Footer.section";
import React from "react";

interface InstaFeedProps {
  posts: InstaItem[];
}

const Instafeed: React.FC<InstaFeedProps> = ({ posts }) => {
  return <div></div>;
};

export default Instafeed;
